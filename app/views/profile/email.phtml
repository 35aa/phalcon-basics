<?php use Phalcon\Tag as Tag; ?>
<h1 class="page-header">Profile::Add new email</h1>
<?=Tag::form(array($this->view->form->getAction())) ?>

	<p>
		<label for="email">New Email<?php if ($form->hasMessagesFor('email')) { ?> (<strong style="color:red;"><?php echo $form->getMessagesFor('email')[0]?></strong>)<?php } ?>:</label><br />
		<?php echo $form->render('email') ?>
	</p>

	<p>
		<?php echo $form->render('submit') ?>
	</p>

<?=Tag::endForm ()?>
