<?php use Phalcon\Tag as Tag; ?>
<h1 class="page-header">Profile::Change username</h1>
<?=Tag::form(array($this->view->form->getAction())) ?>

	<div class="form-group">
		<label for="<?=$form->get('name')->getName()?>"><?=$form->get('name')->getLabel()?>:</label>
		<?php echo $form->render('name') ?>
		<span class="help-block">
<?php
	if (isset($this->view->getVar('errors')[$form->get('name')->getName()])) {
		echo $this->view->getVar('escaper')->escapeHtml($this->view->getVar('errors')[$form->get('name')->getName()]);
	}
?>
		</span>
	</div>

	<div class="form-group">
		<?php echo $form->render('submit') ?>
	</div>

<?=Tag::endForm ()?>
